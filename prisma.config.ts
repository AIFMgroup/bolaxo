// Prisma config file (Prisma 6+) to avoid deprecated package.json#prisma config.
// See: https://pris.ly/prisma-config
import { defineConfig } from 'prisma/config'

export default defineConfig({
  schema: 'prisma/schema.prisma',
})


