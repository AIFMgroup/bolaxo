# Copy to .env.local for development or configure these as secrets in your host (Railway/Vercel).

# --- Core ---
NODE_ENV=development
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Database (Prisma)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DB?schema=public

# Auth / sessions
JWT_SECRET=change-me-to-a-long-random-secret

# --- Email (Brevo / Sendinblue) ---
BREVO_API_KEY=
EMAIL_FROM=noreply@bolaxo.com
EMAIL_FROM_NAME=BOLAXO

# --- Cron protection (for /api/cron/*) ---
CRON_SECRET=change-me

# --- Rate limiting (Upstash) ---
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# --- Sentry (optional) ---
# Client DSN must be public:
NEXT_PUBLIC_SENTRY_DSN=
# Optional server-side DSN:
SENTRY_DSN=
# Sampling knobs (optional)
SENTRY_TRACES_SAMPLE_RATE=0.1
SENTRY_REPLAYS_SESSION_SAMPLE_RATE=0.0
SENTRY_REPLAYS_ON_ERROR_SAMPLE_RATE=0.1
SENTRY_SEND_DEFAULT_PII=false

# --- AI / LLM providers (optional, depending on features used) ---
OPENAI_API_KEY=
AWS_REGION=eu-north-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# --- File storage (optional, depending on features used) ---
AWS_S3_BUCKET=
AWS_S3_REGION=eu-north-1



